{
  "name": "lang-chain-app",
  "private": true,
  "description": "Invoice extraction + natural-language querying demo (FastAPI + React + LangChain)",
  "scripts": {
    "dev:backend": "cd backend && .venv/Scripts/python -m uvicorn app.main:app --reload --port 8000",
    "dev:frontend": "cd frontend && npm run dev",
    "test:backend": "cd backend && .venv/Scripts/python -m pytest -q",
    "test:frontend": "cd frontend && npm run typecheck && npm test",
    "seed": "cd backend && .venv/Scripts/python -m app.seed --force"
  }
}
